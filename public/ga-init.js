/* GA init – legacy loader disabled (use AnalyticsConsentWrapper) */
(function () {
  if (typeof window === "undefined") return;

  // Bu dosya artık sadece geriye dönük uyumluluk için tutuluyor.
  // GA script yükleme işlemi AnalyticsConsentWrapper tarafından yönetiliyor.
})();
